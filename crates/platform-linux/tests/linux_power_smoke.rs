//! Integration smoke test for Linux power listener — requires `UPower` (v1).

#[test]
#[ignore = "requires UPower and hardware; enable when adapter is implemented"]
fn linux_power_smoke_placeholder() {}
